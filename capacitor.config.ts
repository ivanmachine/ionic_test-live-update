import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'live-update-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
